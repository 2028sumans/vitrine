// Dashboard is publicly accessible — Pinterest auth is optional and handled
// inline on the dashboard itself (connect button inside the Pinterest block).
// No route protection needed.
export const config = { matcher: [] };
